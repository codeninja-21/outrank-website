import ContactSection from "../organisms/contact/contact";

export default function Contact() {
    return (
        <main className="flex flex-1 w-full flex-col items-center justify-between bg-neutral-950">
            <ContactSection/>
        </main>
    );
}